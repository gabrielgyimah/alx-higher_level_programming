#!/usr/bin/node

const stringToPrint = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
let counter = 0;

while (counter < stringToPrint.length) {
  console.log(stringToPrint[counter]);
  counter++;
}
